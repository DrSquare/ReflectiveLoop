# logs/

Daily work logs, one file per day/agent/host:
`{YYYY-MM-DD}-{agent}-{host}.md`. This is the only narrative record of what
changed in the wiki and why — see `AGENTS.md` for the full log schema.

Audit reports meant to be read by a person live in `logs/reports/`, not
mixed in with the dated daily logs.
