# indexes/

Generated per-category page listings (finer-grained than the single
`index.md` catalog). Not yet needed while `wiki/` has only a handful of
categories — `index.md` covers it. Add per-category files here once a
category grows large enough that `index.md` becomes unwieldy.
